{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://sozledger.com/schemas/webhook-events.json",
  "title": "WebhookEvent",
  "description": "Webhook event envelope used by the Soz Ledger protocol to notify subscribers of state changes.",
  "type": "object",
  "required": ["id", "type", "timestamp", "data"],
  "properties": {
    "id": {
      "type": "string",
      "format": "uuid",
      "description": "Unique identifier for this event."
    },
    "type": {
      "type": "string",
      "enum": [
        "promise.created",
        "promise.fulfilled",
        "promise.broken",
        "promise.expired",
        "score.updated",
        "evidence.submitted"
      ],
      "description": "The type of event that occurred."
    },
    "timestamp": {
      "type": "string",
      "format": "date-time",
      "description": "ISO 8601 timestamp of when the event was emitted."
    },
    "data": {
      "type": "object",
      "description": "Event-specific payload. Structure depends on the event type."
    }
  },
  "additionalProperties": false
}
